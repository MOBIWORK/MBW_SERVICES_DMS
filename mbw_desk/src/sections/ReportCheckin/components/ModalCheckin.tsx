import { Modal } from "antd";
import styled from "styled-components";

export const ModalDetail = styled(Modal)`
  .ant-modal-content{
    padding: 0px;
    border-radius: 12px;
    overflow: hidden;
  }
`;